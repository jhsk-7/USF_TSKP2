let partyLocation = "Garden"; //String - the message could change.
let shiftValue = 3; //Number - you don't wnat the shift value to change.
let encryptedMessage = ""; //String - encrypted message will be updated when the message is coded.
let decryptedMessage = ""; //String - decrypted message will be updaetd when the message is decrypted.
let isPartySafe = false; //Boolean 

console.log(Number.isInteger(shiftValue))